plugins {
    id 'java'
    id "io.papermc.paperweight.userdev" version "2.0.0-beta.14"
    id "xyz.jpenilla.run-paper" version "2.3.0"
    id "io.freefair.lombok" version "8.7.1"
    id "com.gradleup.shadow" version "9.0.0-beta12"

    // kotlin
    id 'org.jetbrains.kotlin.jvm' version '1.9.20'
}

repositories {
    mavenCentral()
    maven {
        name = "papermc-repo"
        url = "https://repo.papermc.io/repository/maven-public/"
    }
    maven {
        name = "sonatype"
        url = "https://oss.sonatype.org/content/groups/public/"
    }
    maven {
        url = "https://maven.pkg.github.com/theTd/tclite"
        mavenContent {
            includeGroup("com.mineclay")
            includeGroup("com.mineclay.tclite")
        }
        credentials {
            username = "$gprUser"
            password = "$gprKey"
        }
    }
    maven {
        url = "https://maven.mineclay.com/repository/zhuagroup/"
        mavenContent {
            includeGroup("com.mineclay.nativeutil")
        }
        credentials {
            username = "$clayUsername"
            password = "$clayPassword"
        }
    }
}

dependencies {
    paperweight.paperDevBundle("1.21.4-R0.1-SNAPSHOT")
    implementation("org.jooq:joor-java-8:0.9.15")
    implementation("xyz.jpenilla:reflection-remapper:0.1.1")
    implementation("com.mineclay:tclite:1.10.0-SNAPSHOT") {
        exclude(group: "com.github.cryptomorin")
    }
//    compileOnly "io.papermc.paper:paper-api:1.18.2-R0.1-SNAPSHOT"
}

java {
    toolchain.languageVersion.set(JavaLanguageVersion.of(21))
    withSourcesJar()
}

kotlin {
    sourceSets {
        main {
            kotlin.srcDir("src/main/java")
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

tasks.named("assemble").configure {
    dependsOn("reobfJar")
}

processResources {
    def props = [version: version]
    inputs.properties props
    filteringCharset 'UTF-8'
    filesMatching('plugin.yml') {
        expand props
    }
}
